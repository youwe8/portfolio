import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Community & Social | Yuvaraj R",
  description: "Game nights, sports days, and community events hosted by Yuvaraj R in Bengaluru.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
